export class SpeakerModel {
    constructor(public name: string, public jobtitle:string, public bio: string, public image: string, 
                public twitterHandle: string, public blog: string) {

    }
}